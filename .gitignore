__pycache__/
.venv/
.ruff_cache/
.pytest_cache/
scratch/
preview/
preview_local/
preview_local_wasm/

# Internal local-only artifacts (kept on disk, not tracked).
docs/*.png
docs/*report*
docs/*checklist*
docs/*
scripts/screenshot*.py

# data/*.npz are normally cache, but the molab/GitHub-static-preview deployment
# needs them in the repo so the notebook can load without running the precompute
# script first. Tracked explicitly via `git add -f data/*.npz` in CI.
